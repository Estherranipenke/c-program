#include<stdio.h>
void main()
{
	int side,area,perimeter;
	side=10;
	area=side*side;
	perimeter=4*side;
	printf("area of square with side %d is %d",side,area);
	printf("\narea of perimeter with side %d is%d",side,perimeter);
}
